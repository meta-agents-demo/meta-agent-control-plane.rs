# Meta-Agent Control Plane

Rust infrastructure for observable AI-agent progress, structured metacognition, bounded learning, and human oversight.

The implementation is developed under Linear issue `DEN-1057` for `meta-agents-demo/meta-agent-control-plane.rs`.
