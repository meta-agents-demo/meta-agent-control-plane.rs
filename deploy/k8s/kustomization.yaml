# Namespace-scoped application resources only; tenant/Argo ownership stays in k8s-cluster.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - secrets.externalsecret.yaml
  - fleet.yaml
  - networkpolicy.yaml
patches:
  - path: hardening-patch.yaml
